# User Instructions

Add user preferences and personal instructions here when they complement BRAIN's structural rules.

These instructions are returned by `get_rules` and take priority over global rules when they specify or override expected behavior.
